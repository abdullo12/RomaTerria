import React from 'react';

interface DraftsProps {
  isDarkMode: boolean;
}

const Drafts: React.FC<DraftsProps> = ({ isDarkMode }) => {
  return (
    <div className="w-full h-screen bg-white">
    </div>
  );
};

export default Drafts;
